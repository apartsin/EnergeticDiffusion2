# T4 launch notes

NOT LAUNCHED.  See `PRE_FLIGHT.md` for the literature search and the
negative finding (no 1,2,3,5-oxatriazole-class compound with both
experimental rho_exp and experimental D_exp located in the open
literature, April 2026).

The Modal launcher (`modal_t4_oxatriazole_anchor.py`) is committed in a
deliberately-disabled form so that:

  - the bundle structure mirrors T1/T2/T3 for the catalogue,
  - the script is ready to activate as soon as a viable anchor
    compound surfaces (e.g. through a future synthesis paper).

To activate, see the docstring at the top of
`modal_t4_oxatriazole_anchor.py` and the activation section at the
end of `PRE_FLIGHT.md`.
